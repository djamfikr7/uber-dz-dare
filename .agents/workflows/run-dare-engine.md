# Antigravity Production Deployment Blueprint Engine Hook

Execute this exact automation loop to build out the application components, run stress testing scripts, and refactor code paths recursively:

```sequence
1. Read the parameters inside `vault/business_rules.md` and build out the type-safe definitions for the Isar schemas.
2. Initialize background compiler processes to generate serialization bindings: `flutter pub run build_runner build --delete-conflicting-outputs`.
3. Launch the parallel multi-agent system test rig via terminal sub-processes: `node simulator_orchestration.js`.
4. Capture any thrown errors or validation mismatches, trace them back to the source files, and deploy clean architecture code patches.
5. Loop back and repeat the cycle until the simulation executes 10,000 parallel transaction profiles with zero data integrity exceptions.
```
